// 상근이의 여행 
// https://www.acmicpc.net/problem/9372

#include <bits/stdc++.h>

#define ll long long

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);

  int t;
  cin >> t;
  while (t--) {
    int n, m;
    cin >> n >> m;
    while (m--){
      int a, b;
      cin >> a >> b;
    }
    cout << n - 1 << "\n";
  }

  return 0;
}