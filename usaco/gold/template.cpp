// (Gold)
// https://usaco.org/

#include <bits/stdc++.h>

#define ll long long

using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  cout.tie(nullptr);
  
  // ifstream fin("input_file.in");
  // ofstream fout("output_file.out");

  return 0;
}